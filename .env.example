# ============================================
# =========== BOT_INTELIGENTE .env ===========
# ============================================

# --- Binance Testnet (usar testnet por defecto) ---
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=

# --- Símbolos & Mercado ---
# Regla del proyecto: SIEMPRE cotizar contra USDC (no USDT).
# El valor por defecto del símbolo DEBE terminar en /USDC.
DEFAULT_SYMBOL=XRP/USDC

# --- Cliente CCXT ---
ENABLE_RATE_LIMIT=true
TIMEOUT_MS=20000
# Modo sandbox (true para usar entornos de prueba si el exchange lo soporta)
SANDBOX_MODE=true

# --- Logging (opcional; solo si quieres parametrizar) ---
# Nivel de log: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Rotación del log (formato Loguru, p.ej. tamaño o frecuencia):
#   - "10 MB"  -> rota al superar 10 MB
#   - "00:00"  -> rota cada día a medianoche
LOG_ROTATION="10 MB"

# Retención del log (formato Loguru, cuánto histórico conservar):
#   - "7 days"  -> conserva 7 días
#   - "10 files"-> conserva 10 archivos rotados
LOG_RETENTION="7 days"

# (Opcional) Ruta del archivo de log; si se omite, solo salida por consola.
# LOG_FILE=./data/logs/bot.log
